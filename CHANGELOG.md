# 更新日志

## [未发布] - 2026-07-02

### 新增
- **多租户独立发送邮箱配置**：每个租户可在"全局管理"中独立配置 Telegram 认领功能的发件人邮箱
  - 适合多租户场景，每个租户使用自己域的邮箱发送验证码
  - 向后兼容：保留原全局配置作为后备（优先使用租户级配置）

- **邀请码自定义前缀**：在"设置 → 界面定制"中配置邀请码前缀
  - 例如配置 `CF-M365-`，生成的邀请码为 `CF-M365-Hk7m9XpN`
  - 便于品牌标识、活动区分、渠道标记
  - 前缀参与去重检测，确保唯一性

- **TG Bot 管理员生成邀请码**：管理员可在 Telegram 中使用 `/geninvite` 生成邀请码
  - 在"设置 → Telegram Bot 注册"中配置管理员 Telegram ID
  - 支持多管理员独立操作
  - 移动端友好，无需登录后台
  - 生成逻辑与后台一致（加密随机数、去重、自动应用前缀）
  
### 改进
- **邀请码生成功能全面升级**：
  - 使用加密安全的 `crypto.getRandomValues()` 替代 `Math.random()`
  - 移除易混淆字符（0/O、1/I/l 等），提升可读性
  - 添加去重检测，确保邀请码唯一性
  - 添加长度限制（6-32位）和数量限制（最多100个）
  - 简化特殊符号集，移除可能在某些场景下引起问题的符号
  - 默认长度从 16 调整为 12（更易读，仍然安全）
  - 特殊符号默认不勾选

- **邀请码列表默认排序优化**：
  - 默认按生成时间倒序排列（最新的在最前面）
  - 方便快速查看和分发新生成的邀请码
  - 仍可手动切换为其他排序方式

### 变更
- Telegram Bot 设置中移除全局发件人邮箱配置项，改为在各租户中独立配置
- 全局管理界面新增"Telegram 认领验证码发件人邮箱"字段
- 邀请码生成界面添加限制说明和提示文字
- Telegram Bot 设置新增"管理员 Telegram ID"配置项

### 技术细节
#### 多租户发送邮箱
- `globals` 数据结构新增 `senderEmail` 字段
- `graphSendClaimCode` 函数优先使用租户级 `senderEmail`，向后兼容全局配置
- API 端点 `/api/globals` 的 POST/PATCH 支持 `senderEmail` 字段

#### 邀请码前缀
- 配置字段：`inviteCodePrefix`（字符串，可为空）
- 生成时自动拼接：`prefix + randomCode`
- 前缀参与去重检测
- TG Bot 生成也会自动应用前缀

#### 邀请码生成
- 字符池调整：
  - 大写：`ABCDEFGHJKLMNPQRSTUVWXYZ`（移除 I, O）
  - 小写：`abcdefghjkmnpqrstuvwxyz`（移除 i, l, o）
  - 数字：`23456789`（移除 0, 1）
  - 特殊符号：`@#$%^&*-_=+`（简化）
- 安全限制：长度 6-32 位，数量最多 100 个，重试最多 50 次
- 碰撞检测：使用 Set 数据结构自动去重

#### TG Bot 管理员功能
- 配置字段：`telegram.adminTgIds`（字符串数组）
- 新命令：`/geninvite`
- 权限检查：`adminTgIds.includes(String(userId))`
- 生成流程：选择订阅 → 输入数量 → 输入限制 → 生成邀请码
- 固定参数：长度 12 位，字符集（大写+小写+数字）
